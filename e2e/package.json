{
  "scripts": {
    "openui": "cypress open",
    "start": "cd ../agilelink-ci/ && docker-compose up --build -d && cd ../e2e/ && cypress run"
  },
  "devDependencies": {
    "@simonsmith/cypress-image-snapshot": "^9.1.0",
    "cypress": "^13.13.1",
    "typescript": "^5.5.3"
  }
}
