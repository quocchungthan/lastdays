{
  "scripts": {
    "open": "cypress open --config-file cypress.local.config.ts",
    "test": "cypress run --headless --config-file cypress.local.config.ts",
    "open:gherkin": "cypress open --config-file cypress.gherkin.local.config.ts",
    "test:gherkin": "cypress run --headless --config-file cypress.gherkin.local.config.ts",
    "start:regular": "cd ../agilelink-ci/ && docker-compose up --build -d && cd ../e2e/ && cypress run --headless",
    "start:gherkin": "cd ../agilelink-ci/ && docker-compose up --build -d && cd ../e2e/ && cypress run --headless --config-file cypress.gherkin.config.ts",
    "start": "cd ../agilelink-ci/ && docker-compose up --build -d && cd ../e2e/ && cypress run --headless &&  cypress run --headless --config-file cypress.gherkin.config.ts"
  },
  "devDependencies": {
    "@simonsmith/cypress-image-snapshot": "^9.1.0",
    "@types/cypress-cucumber-preprocessor": "^4.0.5",
    "cypress": "^13.13.1",
    "cypress-cucumber-preprocessor": "^4.3.1",
    "typescript": "^5.5.3"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": false,
    "stepDefinitions": "cypress/e2e/step_definitions/"
  }
}
